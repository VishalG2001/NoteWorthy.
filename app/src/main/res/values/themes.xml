<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Noteworthy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/appmain</item>
        <item name="colorPrimaryVariant">@color/appmain</item>
        <item name="android:statusBarColor">@color/appmain</item>
    </style>

    <style name="Theme.Noteworthy" parent="Base.Theme.Noteworthy" />
</resources>